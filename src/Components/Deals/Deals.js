import React from 'react';

const Deals = () => {
    return (
        <div>
            <h2 className="text-center text-danger">No deals are available now</h2>
        </div>
    );
};

export default Deals;